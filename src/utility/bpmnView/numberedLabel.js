//numbered label for the new child that will be added
//used to assign default name to swimlane and milestone added
export function numberedLabel( parentCell , prefix  , number){

    return prefix + "_" + number;
}